# 多人同步 · 方案选型

> **⚠️ 这份是选型讨论的存档,决策已经做完了,代码也照着落地了。**
>
> **最终选定:方案 C(离线优先 + 手动云同步)+ GitHub 后端 + 按条目合并 + 稳定 ID 改造。**
>
> **要动手配的话看 [README 第五节「多人同步」](README.md)**,那里是完整的操作步骤。
> 这份文档留着是为了回答「当初为什么不选 Firebase / 不做实时」这类问题,不用再读一遍才会用。

## 落地情况(2026-08-04)

| 当初的决定 | 实际做成什么样 |
|---|---|
| 方案 C:离线优先 + 手动同步 | 底栏 **☁ 同步** 按钮,显示上次同步时间;⚙ 里可勾「打开 App 时如果有网自动拉一次」 |
| 后端用 GitHub | `sync.js` 走 Contents API 读写私有仓库里的 `ledger.json`。**每次同步是一次 commit,误删能从历史里翻回来** |
| 按条目合并 | 按 `id` 取并集,同 id 比 `mt` 取新的;同毫秒撞车按设备号裁决;删除走墓碑 `del` |
| 稳定 ID 改造 | 数据升到 v4:每条带 `id / mt / by / del / seq`;支出的 `payer`、`share`、`dayId` 全部引用 id。**老数据(v3)打开时自动迁移,不用手动导出重导** |
| 统一结算代表 | 成员可选 `settleTo` 稳定 id,关系随成员条目一起按 `mt/by` 合并。个人账目与真实付款人不变,只有最终转账先按代表聚合;支持多人一组、链式绑定,并对并发循环做确定性兜底 |

下面这几个当初担心的问题,实现时的处理:

- **令牌不能进仓库** —— 每个人在 ⚙ 里手填一次,只存各自手机的 localStorage,永不上传。配的是 fine-grained token,只授权那一个数据仓库的 Contents 读写。
- **两个人同时提交会不会覆盖** —— 不会。Contents API 的 `sha` 就是乐观锁,撞车 GitHub 直接回 409,代码会重新拉取合并再推,最多重试 3 次。
- **合并会不会越合越多** —— 不会。内置数据的 id 是写死的常量,5 个人各自打开拿到的是同一份,合并是干净的无操作。

---

> 以下是当初的选型讨论原文。

> 你之前选的是「想要真正的多人同步」。动手之前先把选项摆出来 —— 因为这件事有个**反直觉的结论**,看完再定不迟。

---

## 一、先说结论

**「实时多人协同」在这趟旅行里大概率是负收益。** 理由不是做不出来,是场景对不上:

1. **同步要网,而这趟最需要记账的地方恰恰没网。** 喀纳斯村里、禾木、阿禾公路,信号是「有一格但发不出微信」的程度。实时协同在弱网下的典型表现是转圈、写入失败、状态不一致 —— 比不同步更糟。
2. **实时协同的价值在「同一秒同时改同一条」。** 5 个人 9 天,真实场景是「谁付了钱谁晚上回酒店补一条」,冲突概率接近 0。为一个不会发生的问题付架构代价不划算。
3. **国内可用的免费后端很少,能用的都要实名/绑账号**,而这只是一趟旅游。

**推荐:方案 C —— 离线优先 + 手动云同步。**
平时纯离线(跟现在一样,该怎么用怎么用),有信号时点一下「⬆上传 / ⬇下载」,按条目合并。
拿到实时方案 90% 的实际价值,没有「没网就废」的风险。

如果看完还是想要真正的实时,直接跳到 [第六节](#六如果你就是要实时)。

---

## 二、后端选型(存哪儿)

| 方案 | 国内能不能用 | 要不要实名/备案 | 花钱 | 评价 |
|---|---|---|---|---|
| **Firebase** | ❌ **不能** | — | 免费额度大 | `googleapis.com` / `firebaseio.com` 在大陆是墙掉的。同伴不挂梯子一个字都同步不了。**直接排除。** |
| **Supabase** | ⚠️ 看运气 | 不用 | 免费额度够 | 部署在 AWS + Cloudflare,没被明令墙,但大陆访问时好时坏。旅途中断了没人能修。**不建议赌。** |
| **腾讯云 CloudBase(云开发)** | ✅ 稳 | 要实名(你已有的话就够) | 有免费额度,超了几块钱/月 | **国内最靠谱的选项。** 有 Web SDK、匿名登录、文档数据库,还支持实时监听。缺点是要开通、控制台有点绕。 |
| **腾讯云 COS / 阿里云 OSS**(当一个云端文件用) | ✅ 稳 | 要实名 | 几分钱 | 只存一个 `data.json`,前端直接读写。最简单,但**签名密钥不能放前端**,得开临时密钥或做成公共读+私有写(那就得有个写入口)。适合搭配方案 B/C。 |
| **GitHub 仓库当数据库**(用 API 提交 json) | ⚠️ 慢但通 | 不用 | 免费 | 不用另开服务,但**写入要 token,token 放在前端就等于公开** —— 任何人能改你的仓库。除非只做「一个人写、其他人读」。<br>**← 最终选的就是这个。** token 不写进前端代码,改成每人在 ⚙ 里手填、只存自己手机;再配一个只授权单个私有仓库的 fine-grained token,泄露的最坏结果就是那本账被人改,改了也能从 commit 历史翻回来。 |
| **不用后端**(现状:导出/导入 json) | ✅ | 不用 | 免费 | 零风险零成本,代价是手动。 |

---

## 三、同步策略(怎么合)

后端选哪个其实是次要的,**真正决定体验的是合并策略**:

### A. 整档覆盖(last-write-wins)

上传 = 把我的全量数据盖上去,下载 = 用云端全量盖掉我的。

- ✅ 实现最简单,一个 json 文件就够
- ❌ **会丢数据**:巴巴在禾木离线加了一条,njw 在阿勒泰离线加了一条,谁后上传谁把对方的抹掉
- 只适合「明确只有一个人记账」的场景

### B. 按条目合并(推荐)

给每条支出一个唯一 id,同步时按 id 取并集;同一条 id 两边都改过,取时间戳新的。删除记 tombstone(墓碑)而不是直接删,否则删掉的条目会被别人的旧数据「复活」。

- ✅ 各自离线记账,回头一同步全都在
- ✅ 不需要实时连接,弱网友好
- ⚠️ 需要先做[数据模型改造](#四必须先做的改造稳定-id)
- 实现量:中等

### C. 实时协同

监听云端变更,别人一改我这边立刻刷新。

- ✅ 体验最好(有网的时候)
- ❌ 弱网下体验最差
- ❌ 需要处理在线/离线状态、写入失败重试、本地队列
- 实现量:大

---

## 四、必须先做的改造:稳定 ID

> ✅ **这一节已经做完了。** 下面描述的是改造前的样子和为什么要改;改完之后的结构见 [README「数据结构(v4)」](README.md)。

**这是所有多人方案的前置条件,现在的数据结构过不了这关。**

看 `data.js`,当前:

```js
members: ["巴巴", "柠檬", "njw", "老A", "阿菜"],
expenses: [
  { t: "亚朵酒店 · 巴巴那间", amt: 357, payer: "巴巴",
    share: ["巴巴","njw","老A","阿菜"], day: "D1", src: "room" },
  ...
]
```

问题:

1. **`payer` / `share[]` 用的是人名字符串。** 只要有人把「老A」改成「老 A」(多个空格)或者改成真名,他名下所有支出的分账关系当场断掉 —— 单机也有这问题,多人同步会放大成灾难。
2. **`expenses[]` 每条没有 id。** 合并时无法判断「这是同一条被改了」还是「这是两条不同的」,只能靠下标 —— 而下标在两个人各自增删后必然错位。
3. **`days[]` 用 `b:"D1"` 当键**,这个还行(天数不太会变),但如果有人插了一天就全乱。

改法:

```js
members: [
  { id: "m1", name: "巴巴" },
  { id: "m2", name: "柠檬" },
  ...
],
expenses: [
  { id: "e_k3f9a2",           // 建时生成,永不变
    t: "亚朵酒店 · 巴巴那间",
    amt: 357,
    payer: "m1",              // 存 id,不存名字
    share: ["m1","m3","m4","m5"],
    day: "D1", src: "room", manual: true,
    mt: 1754000000000,        // 最后修改时间戳,合并时比大小
    by: "m1"                  // 谁改的,方便出问题时追责(?)
  },
  ...
]
```

配套:导入老 json 时做一次迁移(按名字映射到 id),这样你现有的数据不会白录。

**这块改造本身就有独立价值** —— 改完之后单机版改名字也不会炸了。

---

## 五、推荐方案的落地样子

**后端**:腾讯云 CloudBase(或者 COS + 一个云函数写入口)
**策略**:B 按条目合并
**交互**:

- 底栏加一个「☁ 同步」按钮,旁边显示「上次同步:今天 21:03」
- 点一下 = 拉云端 → 合并 → 推回去,3 秒内完事
- 没网时点它 → 提示「现在没网,有信号了再点」,**不影响任何本地操作**
- 可选:每次改动后如果有网,静默同步一次(有网就自动,没网就攒着)

**同伴那边的心智负担**:0 → 该怎么用怎么用,晚上回酒店有 wifi 了顺手点一下同步。

**要你做的事**:

1. 开通腾讯云 CloudBase(实名认证 + 建一个环境,免费额度够用)
2. 把环境 ID 发我,我把同步逻辑接进去
3. 顺带做完稳定 ID 改造 + 老数据迁移

---

## 六、如果你就是要实时

也能做,在推荐方案基础上加:

- CloudBase 数据库的 `watch()` 实时监听,别人改了我这边自动刷新
- 本地写入队列:没网时把操作攒起来,来网了按顺序回放
- 在线状态指示(顶栏一个小绿点/灰点)
- 冲突提示:「巴巴刚也改了这条,已用较新的」

额外代价:

- 实现量大概是方案 C 的 3 倍
- **需要真机在弱网下测**,headless 浏览器测不出「有一格信号但发不出去」这种状态
- 旅途中一旦出问题(SDK 报错、额度超了、环境被停),没有降级路径 —— 除非同时保留手动同步当兜底

如果要做,我建议**先做方案 C 跑通,再把实时监听叠上去**,这样至少有个能用的底。

---

## 七、还有一个「零成本」的中间选择

如果不想开云服务:

**指定一个人当账本管理员**(比如你),其他人只管把小票发群里 / 口头报一句,管理员统一录。
每天晚上管理员「导出」一份 json 发群里,谁想看细账就自己「导入」看一眼。

- ✅ 今天就能用,零改造
- ✅ 完全没有网络依赖
- ❌ 管理员累一点
- ❌ 别人看到的是快照,不是最新的

说实话,**5 个人 9 天,这个方案的实际体验可能不比方案 C 差多少。**

---

## 需要你定的(已定,存档)

| 当初的问题 | 你的答复 |
|---|---|
| 1. 走哪个方案? | **方案 C** |
| 2. 后端? | **GitHub**(所以第 2 问的「腾讯云要实名」不用管了) |
| 3. 合并粒度? | **按条目合并** |
| 4. 稳定 ID 改造现在做吗? | **做** |

四条都已实现并验证。接下来只剩配置动作:建私有数据仓库 → 发 fine-grained token → 每人在 ⚙ 里填一次。
步骤见 [README 第五节](README.md)。
